#include <iostream>
using namespace std;

// Min heap - smallest at top
void heapify(int theNumbers[])
{
    // Start at one (parent)
    // if left-child < parent
      // swap left-child and parent
    // if right-child < parent (which could now be the old left-child)
      // swap right-child and parent
      
}

int main() 
{
    cout << "Foo" << endl;
}